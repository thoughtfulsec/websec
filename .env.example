# Environment Configuration
# This is the ONLY .env file for the project (located in the root directory)
# Both Docker Compose and local development scripts use this file
#
# To use: Copy this file to .env and fill in your values
# cp .env.example .env

# Google OAuth Configuration
# Get these credentials from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials

# Your Google OAuth Client ID
# Example: 123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# Your Google OAuth Client Secret
# Example: GOCSPX-abcdefghijklmnopqrstuvwxyz
GOOGLE_CLIENT_SECRET=your-client-secret-here

# OAuth Callback URL
# This must match exactly what you configured in Google Cloud Console
# For local development, use:
GOOGLE_CALLBACK_URL=http://localhost:8888/auth/google/callback

# Session Secret
# Generate a random secret key for session encryption
# You can generate one with: openssl rand -base64 32
# IMPORTANT: Use a strong random value in production!
SESSION_SECRET=change-this-to-a-random-secret-key

# JWT Signature Verification Keys (ES256 - ECDSA P-256)
# These keys are used to verify JWT signatures in submitted entries

# Production Public Key (used to verify production JWTs)
JWT_PUBLIC_KEY_PROD="production pubkey value here"

# Local Development Keys (for testing JWT generation/verification)
# Generate a new key pair with:
#   openssl ecparam -name prime256v1 -genkey -noout -out jwt-dev-private.pem
#   openssl ec -in jwt-dev-private.pem -pubout -out jwt-dev-public.pem
# Then copy the contents here (keep the quotes and newlines)

# Development Public Key (used to verify locally-generated JWTs)
JWT_PUBLIC_KEY_DEV="-----BEGIN PUBLIC KEY-----
your public key value from above here
-----END PUBLIC KEY-----"

# Development Private Key (NEVER commit this to git! Only for local testing)
# Used by the generate-jwt script to create test tokens
# IMPORTANT: Keep this secret! Add .env to .gitignore
JWT_PRIVATE_KEY_DEV="-----BEGIN EC PRIVATE KEY-----
your private key value from above here
-----END EC PRIVATE KEY-----"

